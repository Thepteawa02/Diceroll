import 'package:flutter/material.dart';

class Stylez extends StatelessWidget{
  Stylez({super.key});

  Widget build(context){
    return 
    Text(
      'Hello World',
      style:TextStyle(
        fontSize: 48,
        color: Colors.white
      )
    );
  }
}